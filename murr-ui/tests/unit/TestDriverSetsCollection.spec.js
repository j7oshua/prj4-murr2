import { shallowMount } from '@vue/test-utils'
import DriverSiteReport from '@/components/DriverSiteReport'
import { expect } from 'chai'

// global variable for wrapper container
describe('DriverSiteReport.vue', () => {
  // before each test

  // create a new wrapper container for the shallow mount

  /**
   * Title: Driver submits collected, contaminated, obstructed containers on site 1
   * Purpose: This test will test that ALL fields are correctly inputted and returned valid
   * Expected Result: Success
   * Return: return to collection page
   **/
  it('Should successfully be valid with all fields entered', async () => {

  })

  /**
   * Title: Driver submits three collected containers on site 1
   * Purpose: This test will test if the driver incorrectly inputs a lower number than required
   * Expected Result: Failure
   * Return: Error Message description: "Error - Invalid number of containers".
   **/
  it('Should display error when invalid container number is entered', async () => {

  })

  /**
   * Title: Driver submits no containers on site 1
   * Purpose: This test will test if the driver incorrectly inputs nothing
   * Expected Result: Failure
   * Return: Error Message description: "Error - Invalid require Bin number amount".
   **/
  it('Should display error when container fields are required', async () => {

  })
  /**
  * Title: Driver submits no containers on site 1
  * Purpose: This test will test if the driver incorrectly inputs nothing
  * Expected Result: Failure
  * Return: Error Message description: "Error - Invalid require Bin number amount".
  **/
  it('Should display error when container fields are required', async () => {

  })
})
